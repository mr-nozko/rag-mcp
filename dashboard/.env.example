# RAGMcp Dashboard - Environment Variables
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.

# Path to your ragmcp.db SQLite database file (relative to dashboard/ directory)
DB_PATH=../ragmcp.db

# Port the dashboard will run on (default: 3001)
PORT=3001

# Node environment: "development" or "production"
NODE_ENV=development

# --- Dashboard Authentication ---
# Username for the dashboard login page (default: admin)
ADMIN_USERNAME=admin

# Password for the dashboard login page (REQUIRED - no default)
# Generate a strong password: openssl rand -base64 24
ADMIN_PASSWORD=
